import ParametersApp from "@/components/parameters/ParametersApp";

// No title override: the landing page takes the site title from the root layout.

export default function Page() {
  return <ParametersApp />;
}
